package behaviours;

public interface IPlay {

    // This interface is used to enforce a play() function on classes
    // that implements it. The play() function must return a string
    // of the noise that the instrument creates.

    String play();
}
